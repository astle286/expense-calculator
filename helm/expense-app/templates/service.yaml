apiVersion: v1
kind: Service
metadata:
  name: {{ include "expense-app.fullname" . }}
  labels:
    {{- include "expense-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expense-app.selectorLabels" . | nindent 4 }}
